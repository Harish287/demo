const BaseURL="";


const charges= `${BaseURL}/new/v1/charges`;
const charges1= `${BaseURL}/new/v1/charges`;
const charges2= `${BaseURL}/new/v1/charges`;
const charges3= `${BaseURL}/new/v1/charges`;
const charges4= `${BaseURL}/new/v1/charges`;